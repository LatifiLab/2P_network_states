# This folder contains two simulations files:

- simulation_negative impact on feature: simulate on when adding negative edges in the network, how modularity and community number increase, small-worldness index decrease, clustering coefficient decrease, characteristic path length increase.
- simulation_hub: simulate why in negative networks, the hubs serve both local clustering and global bridging effects
